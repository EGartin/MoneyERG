<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/bannerText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Get Dat Money" />

    <EditText
        android:id="@+id/hoursWorked"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/bannerText"
        android:layout_marginTop="106dp"
        android:layout_toRightOf="@+id/bannerText"
        android:ems="10"
        android:inputType="numberDecimal" >

        <requestFocus />
    </EditText>

    <TextView
        android:id="@+id/hoursWorkedText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/hoursWorked"
        android:layout_alignBottom="@+id/hoursWorked"
        android:layout_alignParentLeft="true"
        android:layout_alignRight="@+id/bannerText"
        android:text="@string/hoursWorked" />

    <EditText
        android:id="@+id/wageRate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/calculateButton"
        android:layout_alignRight="@+id/hoursWorked"
        android:layout_below="@+id/hoursWorkedText"
        android:layout_marginTop="47dp"
        android:ems="10"
        android:inputType="numberDecimal" />

    <TextView
        android:id="@+id/WageRateText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/wageRate"
        android:layout_alignBottom="@+id/wageRate"
        android:layout_alignLeft="@+id/hoursWorkedText"
        android:text="@string/hoursWorked" />

    <Button
        android:id="@+id/calculateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/wageRate"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="36dp"
        android:text="@string/calc" />

    <TextView
        android:id="@+id/resultText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/calculateButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:text="@string/totalWages" />

</RelativeLayout>