<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <EditText
        android:id="@+id/hoursWorked"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="166dp"
        android:layout_marginRight="48dp"
        android:ems="10"
        android:hint="Hours Worked"
        android:inputType="number|numberDecimal" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/calculateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/hoursWorked"
        android:layout_alignTop="@+id/hoursWorked"
        android:layout_marginTop="69dp"
        android:text="@string/calc" />

    <TextView
        android:id="@+id/totalWages"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/calculateButton"
        android:layout_below="@+id/calculateButton"
        android:layout_marginRight="38dp"
        android:layout_marginTop="31dp"
        android:text="@string/totalWages" />

    <EditText
        android:id="@+id/wageAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/hoursWorked"
        android:layout_alignLeft="@+id/hoursWorked"
        android:layout_marginBottom="58dp"
        android:ems="10"
        android:hint="Hourly Rate"
        android:inputType="number|numberDecimal" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/wageAmount"
        android:layout_alignLeft="@+id/wageAmount"
        android:text="Hourly Rate" />

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/hoursWorked"
        android:layout_alignLeft="@+id/hoursWorked"
        android:text="Hours Worked" />

</RelativeLayout>